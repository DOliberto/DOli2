# Diário Oficial Templates

to build DO articles, we use jinja2 HTML templates plus an additional
markup that specifies which kind of flask/WTForms form will acquire
it. the markup is as in `example.html`.
